import BuyForMeManagement from '@/components/admin/BuyForMeManagement';

export const metadata = {
  title: 'BuyForMe Management - HatHak Admin',
  description: 'Manage BuyForMe requests and fulfillment process',
};

export default function BuyForMePage() {
  return <BuyForMeManagement />;
}